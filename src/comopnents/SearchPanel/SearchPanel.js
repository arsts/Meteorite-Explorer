import React, { Component } from 'react'

export default class SearchPanel extends Component {
  render() {
    return (
      <div>
        <label></label>
        <input type="text" placeholder="Enter search items"></input>
        <button>Search</button>
      </div>
    )
  }
}
